# v0.11.4 — M5 dead-code sweep audit

## Knip baseline (before)

- 1 unused devDependency (`@types/diff`)
- 2 unused exports (`AGENT_SKILLS_PATHS`, `REGISTRY_CHIPS`)
- 7 unused exported types (`AgentDef`, `RescanState`, `RegistrySortBy`, `RegistrySortDirection`, `IPCFailureFields`, `UpstreamProbeCompleteEvent`, `RegistrySource`)
- 2 configuration hints (orphaned `ignore` entries in `knip.json`)

**Total: 12 violations**

## Applied (10)

### Removed entirely

- `@types/diff` devDep — `diff` v9 bundles its own types, the `@types/diff` package is redundant.
- `AGENT_SKILLS_PATHS` const in `agentDisplay.ts` — no callers; never referenced.
- `REGISTRY_CHIPS` re-export alias in `RegistryFilters.tsx` — same name was kept locally as `CHIP_DEFS`.
- `RegistrySource` re-definition in `main.ts:123` — was duplicating the canonical declaration in `ipc.ts`. `main.ts` now imports the type.
- `AgentDef` re-export from `agentDisplay.ts` — agent-display surface only needs `AgentId`.
- `knip.json` `ignore` array — was orphaned (the entries `skills/**` and `docs/meta-schema.json` are no longer paths knip would scan).

### Un-exported (kept internal)

- `IPCFailureFields` interface in `ipc.ts` — only used in same file.
- `RegistrySortBy` / `RegistrySortDirection` types in `RegistryFilters.tsx` — only consumed by the sibling `RegistrySortState` type.

### Converted dynamic imports → static

- `App.tsx`'s `import("./components/Header.js").RescanState` → static `import { type RescanState }`.
- `main.ts`'s two `import("../shared/ipc.js").UpstreamProbeCompleteEvent` sites → static `import { type UpstreamProbeCompleteEvent }`.

Dynamic-import-only references hide identifiers from knip and from grep-based codebase navigation. Static imports surface the dependency in the import block where it belongs.

## Knip baseline (after)

```
> knip

(no output, exit 0)
```

Zero violations.

## specs-code-cleanup pass

Scanned `packages/desktop/src/` for:

- `console.log` debug debris — **none found**
- `TODO` / `FIXME` / `XXX` markers — **none found**
- Commented-out `import` / `export` blocks — **none found**

The renderer surface is already production-clean. Nothing for `specs-code-cleanup` to do at this layer.

## Out-of-scope follow-ups

- `packages/core/dist/` and `packages/desktop/dist-main/` contain build artifacts that knip ignores by config. Those will regenerate on next build. No action.
- `packages/cli` was scanned; no findings. The `cli-minimal` plan is queued separately and will absorb any further pruning there.

## Exit state

- `pnpm knip` clean.
- `pnpm typecheck` green.
- `pnpm build` green.
